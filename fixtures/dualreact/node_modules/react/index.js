// Minimal stand-in: identity + a hook whose dispatcher lives on THIS module instance.
// Two copies of this module => two dispatchers => the classic dual-React "useRef of null".
let dispatcher = null;
const React = {
  __reactModuleId: 'THE-ONE-REACT',
  __setDispatcher(d) { dispatcher = d; },
  useRef(initial) {
    if (!dispatcher) throw new TypeError("Cannot read properties of null (reading 'useRef')");
    return { current: initial };
  },
};
module.exports = React;
